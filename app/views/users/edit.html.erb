
<%= form_with model: @user do |f| %>
<main>
<p id="notice"></p>
<div class="container">
 <div class="row">
  <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm0 mx-auto">
    <h2>User info</h2>
    <% if @user.errors.any? %>
    <%= @user.errors.count %>error prohibited this obj from being saved:
    <ul>
    <% @user.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
    </ul>
<% end %>
<div class="form-group">
  <label for="user_name">Name</label>
  <p><%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"名前"%></p>
</div>
<div class="form-group">
  <label for="inputImage">Image</label>
  <p><%= f.file_field :profile_image, placeholder: "プロフィール画像", accept: "image/*" %></p>
  </diV>
  <div class="form-group">
  <label for="imputintroduction">Introduction</label>.

<p><%= f.text_field :introduction %></p>
  </div>
  <div class="form-group">
  <p><%= f.submit "Update User" %></p>
<% end %>
</div>
  </div>
 </div>
</div>
</main>